# Open questions — where genuine uncertainty lives; spend effort HERE
- Floor #2's TRUE current value: unknown until pre-flight re-runs under
  the finished stack (write-time provenance + taxonomy + symmetric
  context). 26.9% is stale by construction.
- agent_204's 13 figures: unscored pending the fresh symmetric trim
  solve (the last instrument-era item).
- Retroactive-stamp blast radius: how many admissibility verdicts change
  under reconstructed provenance — enumeration ordered, not yet reported.
- The 9 no-counterpart dual figures: real coverage gaps; unverified by
  policy until a derivation exists.
- Class B (596 MCQs): never family/duplicate-clustered; Stage 2/3 sizing
  unknown until that runs.
- agent_303 stem clarification and the ch17 rounding-convention stem
  clauses: ordered as fix classes; APPLICATION not yet confirmed in an
  artifact — verify before those chapters run.
- Countersign packet: must be REGENERATED before the operator session
  (three goldens' truth moved: 283 repaired, 285 passing, 130 re-minted).
- Executor code + evidence bundles are NOT yet in this repo (see README
  join protocol) — only the worker can commit them.
