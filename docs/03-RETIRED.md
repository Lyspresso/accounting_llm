# 03-RETIRED.md — do not re-derive, re-try, or re-believe these
The anti-loop file. If a number or approach below appears in an older
document, it is HISTORY, not state. Quoting a retired number as current,
or re-trying a retired approach without citing this file and new
evidence, is an error.

## Retired numbers (and what replaced them)

| retired | why | current truth |
|---|---|---|
| 67% pilot pass rate | comparator couldn't read LaTeX keys (60.6% invisible) | pass-rate headlines retired entirely (D3); funnel speaks lineages |
| 81 / 84 "corroborated" headlines | instrument-era readings; comparator since rebuilt | (coverage, candidates, findings) triples only |
| **26.9% floor-#2 FP rate** | measured pre-taxonomy, pre-JE-unification, through since-fixed matcher bugs, on the wrong FP definition | UNKNOWN until pre-flight re-runs under the finished stack — the only honest current value |
| 38.5% FP first pre-flight | script declared dual-derivation in the stack and never applied it | superseded same turn |
| 2.4× meter "correction" / 18.8 calls-per-item | parser double-count; failed the wall-clock physics bound | original 50.1k billable/question stood; harness meter authoritative |
| 338 and 671 sentinel readings | counted progress rows, not terminals; rows not items; no scope filter | true terminal count 0; sentinel v2 watches the full status×scope vector |
| 1,950 "distinct items" | counted content hashes | 1,828 lineages is the reporting unit (D17) |
| 0.799 trim cost ratio, +44% answer-count | from trim_ab.js, which ran WITHOUT the coverage instruction | 39,652/q vs 50,091/q from evidence_parity; +44% discarded, not refreshed |
| asymmetric dual results (40/19/4 etc.) | dual had the LO 11-8 note, trim didn't | symmetric run: 18 verified / 19 disagreement / 9 no-counterpart on 11 items; agent_204's 13 still UNSCORED pending fresh symmetric trim solve |
| 2.0% key-silent heuristic | stem heuristic contradicted measured parity data | measured: baseline 7 vs trim 63 key-silent figures per 50 items |

## Retired approaches (tried, failed, replaced)

- Terminal-only sentinel → guarded an empty set; full-vector sentinel v2.
- Retroactive provenance stamping → launders staleness; write-time only.
- Blanket money tolerances (±$1.00 AND naive ±$0.01) → precision-
  inherited tolerance at the key's displayed precision.
- Inferred magnitude scaling (900 ↔ 0.9) → declared unit headers only.
- Order-dependent KEY_SILENT downgrade → deterministic subset-sum.
- Hand-written banned-pattern lists per ruling → one generator, full
  class, completeness assert.
- Per-script hand-written solver rule blocks → one builder, role varies
  one framing line, instruction-region identity probed.
- Exact-string seed membership → normalizer-based membership.
- Corroboration rate as harness quality → D3.
- Pooled z-test across one-per-pack cluster samples → sampling design
  stated before pooling; composition chi-square beside any headline.
- "w"-mode ledger writes anywhere → ledger_io single merge-only API.
- Cowork migration → abandoned by operator; chat + this git is HQ.
- Chunk-pasting large docs through chat → files/git only.

## Retired beliefs (held confidently, reversed by evidence)

- "Trim generates noise; baseline is cleaner." Reversed: baseline was
  under-reporting past defects; trim surfaced every genuine finding.
- "agent_204 proves trim reasons worse." RETRACTED by its author: trim
  lacked the note dual had. Context deficit, not capability (D9, D19).
- "The two-part operating year-end entry is a defect." It is the
  textbook's own blessed alternative (Demo 17-3); net-per-account
  canonicalization handles it.
- "ch17 rounding divergences are errors." Convention pairs, blessed both
  ways — EXCEPT agent_283's $1, which was a real key defect (D8). Both
  facts are true; neither generalizes over the other.
- "More goldens will fix the FP rate." Goldens cannot move the POINT
  ESTIMATE — only comparator/stack work can. Precisely stated
  (REPORT-001): goldens ARE required for the other two blockers —
  interval WIDTH (Wilson upper bound needs n ≥ 35 with zero chargeable)
  and D6 TIER (human countersign). Same belief, three named blockers.
- "The pause list is protecting us from drift." It never gated anything
  until the enforcement probe existed.
- "R&D Expense," "Buildings," "Income Taxes Payable" as corpus account
  names — none occurs; see D15 before "adding" them back.
