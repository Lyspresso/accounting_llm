# Prediction ledger — registered with metric + threshold; scored
- P1 Dual-derivation convergence (trim candidates → baseline's): HELD —
  gap 13 → 2 items; residual dominated by the one known, fixed cause.
- P2 130/283 convert to passes on free re-compare post-key-repair:
  FAILED 0/2 — informative: exposed the un-unified JE comparison path.
- P3 JE-unification movers = operating-lease year-end only: HELD —
  exactly one mover (agent_285), in-class, probes 40/40 unmoved.
- P4 Genuinely-lost rows after both ledger truncations = 0: HELD —
  100/100 scored items recoverable from evidence bundles.
Rule: a ledger where everything passes is itself a red flag; failed
predictions are yield.
