FROM: executor
TO:   reviewer
DATE: 2026-08-10
RE:   Worksheet shipped; v1.4.6 cut; two ORDER-004 expectations did not hold
ANSWERS: ORDER-004
STATUS-NEEDED: yes
NEEDS-OPERATOR: yes  (the worksheet is ready; the session is the only remaining condition)

Seven items. Fresh export in this commit
(`generated: 2026-08-10T04:08:23Z`). **Two predictions in ORDER-004 did not
hold** — items 2 and 2 again — and both are reported as measured rather than
worked around.

---

## 1. RULING RATIFIED — **NOTED**, no action

D10.2 / D23 recorded on your side. `agent_130#00` stays expects-FAIL.

## 2. ALIAS PAIRS (D15.1) — **DONE, but neither predicted effect occurred**

Applied to `pack/account_aliases.csv`: 6 alias forms across 3 canonical
accounts. Base forms canonical, suffixed forms alias in. Queue entries marked
ruled (`out/alias_pair_queue.json`, 3 of 7 now carry a ruling). Verified the
comparator actually loads them:

```
'accounts receivable net'      -> accounts receivable
'common stock no par'          -> common stock
'retained earnings dividends'  -> retained earnings
```

**(a) The unknown-count delta is ZERO.** 843 items / 3,690 unresolved lines,
before and after — not a rounding artifact, literally unchanged.

Cause, measured: those three forms occur **0 times in corpus keys**. They exist
only in one *solver output* (agent_130#00's re-solve). `UNKNOWN_ACCOUNT` counts
**key** accounts absent from the chart, so aliasing a solver-side name cannot
move it. The ruling is still correct and still worth having — it prevents a
future comparison from splitting on those names — but it buys comparator
agreement, not chart coverage, and the delta line in the order expected the
latter.

**(b) `agent_002#03` did NOT clear.** It remains the single finding-bearing
item. Its pair is a *different* one, still unruled:

```
["Investment in AFS <security> Bonds", "Debt Investments—Available for Sale"]   ruling: null
```

queued in REPORT-002 §5 and raised by agent_002; the three approved here came
from agent_130's re-solve. **It needs its own ruling under the dominance rule.**
Measured dominance for it: the key-side form `investment in htm securities
<name> bonds` occurs across many keys; the solver's pooled `debt investments—
available for sale` form occurs **0 times in any key**.

This does not move the gate — those 6 findings are all MATCHER_ARTIFACT and
chargeable stays 0 — so floor #2 still PASSES at CI [0%, 8.8%].

## 3. TREATMENT NOTE (zero-net intra-cash-pool transfers) — **NOT-STARTED, by instruction**

You queued it for tranche-1 prep, not now. Not started. When it lands it needs
builder wiring + a `test_wiring.py` gate entry, same as LO 11-8.

## 4. REGISTRY COMPLETENESS PROBE — **DONE**, `test_registry.py`, in the suite

All 8 `out/evidence_*` directories registered; 0 unregistered; 0 ghosts.
The probe asserts both directions — an unregistered directory (invisible solver
work) **and** a registered-but-absent one (stale, and it silently shifts
resolution order for everything after it). `EXCLUDED` is a dict requiring a
reason, so exclusion is a decision and omission is a bug.

## 5. v1.4.6 — **DONE**, cut with movers enumerated

Owed, and you were right to call it. `skill/releases/v1.4.6/`, changelog entry
carries the movers table:

| change | mover | before → after |
|---|---|---|
| parity_dirs | floor #1 **off the origin machine** | 0/40 unmeasurable → 40/40. **On this machine: no change** |
| make_row `stage: null` | ledger sha, once | inert — `questions.jsonl` byte-identical, 3,167 rows held, decomposition unchanged |
| sentinel hard-import | **none** | constants were already identical; the fallback was a latent second source |
| EVIDENCE_PREFERENCE | clean goldens | 26 → 41 |
| terminal conjunction | **zero rows** | future writes only; verified REFUSED under today's GREEN gate |
| D15.1 aliases | **UNKNOWN_ACCOUNT zero delta** | 843 / 3,690 unchanged — see item 2 |

Four laws folded into both prompts: evidence registry; machine-independent
measurement paths (incl. "zero probes is broken, not failing"); the launch
conjunction; single-homing admits no fallback.

## 6. COUNTERSIGN WORKSHEET — **DONE**, `deliverables/countersign_worksheet.md`

**40 items** — `journal_entry 23` + `numeric 17` — stratified across **14
chapters**, round-robin so no chapter clusters at the tired end of the session.
Generated by `emit_worksheet.py`, wired into `deliver.sh`, so it can never go
stale silently.

Per item: scenario, the Required parts, evidence path, tier, guarded rule, and a
PASS/QUERY box. The design decision worth your review:

**Every figure is shown beside the key text it matched.** "Matches key" is the
claim under audit, so asserting it would defeat the exercise — the operator sees
`121,005.56` next to `…PV **$121,005.56** ≥ 90% × $125,000…` and can agree or
not in seconds.

**Figures that could NOT be paired to key text are listed separately, never
dropped.** They are flagged as the ones deserving attention — the key may be
silent on them, or they may be ours alone. Silently omitting them would hide
exactly what a human is there to catch.

## 7. REPORT-004 + fresh export — **DONE**, this commit

---

## State

| | |
|---|---|
| floor #1 detection | 40/40 = 100%, CI [91.2%, 100.0%] — **PASS** |
| floor #2 chargeable | **0/40** = 0.0%, CI [0.0%, **8.8%**] vs 10% — **PASS** |
| FIRST_BATCH_GATE | **GREEN** |
| certified | **NO** — zero human-tier goldens |
| terminal writes | **REFUSED** (verified live under the GREEN gate) |
| fixtures | **10/10 GREEN**, cold cache |
| ledger | 3,184 rows, 1,828 lineages, terminal count 0 |

**No tranche has run. Nothing is labelled `verified`.**

The worksheet is on disk and the brief is committed. The only remaining
condition is the operator session and the one-line `DECISION-003` it produces.

One open item that is *not* blocking: `agent_002#03`'s alias pair still needs a
ruling. It is the last finding-bearing golden, and it is MATCHER_ARTIFACT — so
it costs nothing at the gate, but it will keep showing up in every offender list
until it is ruled.
